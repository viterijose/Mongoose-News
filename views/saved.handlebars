
<button class = "saved-articles">Saved Articles</button>

<h1>Articles</h1>

{{!--
<ul> --}} {{#each data}} {{!--
  <li>
    <h2 data-id="{{id}}-heading">
      <a href={ {link}}>{{heading}}</a>
    </h2>
    <img data-id="{{id}}-img" src={{thumb}}>
    <p data-id="{{id}}-summary">{{summary}}</p>
    <button class="save-article" data-id="{{id}}">SAVE ARTICLE</button>
  </li> --}}

  <form class="create-form">

    <div class="form-group">

      <a href= {{articleLink}} data-head= {{id}}>{{headline}}</a>

      <img src= {{imgLink}} data-img= {{id}}>
      <p data-sum= {{id}}>{{summary}}</p>

    </div>
    <button class="add-note" data-id={{id}}>ARTICLE NOTES</button>
    <button class="delete-article" data-id={{id}}>DELETE ARTICLE</button>
  </form>
  {{/each}} {{!-- </ul> --}}